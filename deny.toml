# cargo-deny configuration (v2 format, cargo-deny >= 0.16)
# https://embarkstudios.github.io/cargo-deny/

[advisories]
unmaintained = "all"
yanked = "deny"
ignore = [
    # Transitive deps with no safe upgrade available — revisit when upstream updates.
    "RUSTSEC-2024-0436",  # paste (via tokenizers → macro_rules_attribute)
]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "Unlicense",
    "MPL-2.0",                # option-ext (dirs-sys → dirs → hf-hub)
    "CDLA-Permissive-2.0",   # webpki-roots, webpki-root-certs (ureq → ort-sys)
]

[licenses.private]
ignore = true

[bans]
multiple-versions = "warn"
wildcards = "allow"
skip-tree = [
    { crate = "windows-targets" },
]

[sources]
unknown-registry = "warn"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
